import React from 'react';
import Search from '../Search';
function Nav() {
    return (
        <div class="topnav">
            <a className="active" href="#home">LibData</a>
        </div>
    );
}

export default Nav;